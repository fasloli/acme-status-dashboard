[tool.poetry]
name = "status-dashboard"
version = "1.0.0"
description = "Acme Internal Status Dashboard"
authors = ["fasloli"]

[tool.poetry.dependencies]
python = "^3.12"
flask = "3.1.1"
gunicorn = "23.0.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
